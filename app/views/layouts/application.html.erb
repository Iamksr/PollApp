<!DOCTYPE html>
<html>
  <head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
</head>
<style>
  body {font-family: Arial, Helvetica, sans-serif;}

.navbar {
  width: 100%;
  background-color: #555;
  overflow: auto;
}

.navbar a {
  float: left;
  padding: 12px;
  color: white;
  text-decoration: none;
  font-size: 17px;
}

.navbar a:hover {
  background-color: #000;
}

.active {
  background-color: #4CAF50;
}

@media screen and (max-width: 500px) {
  .navbar a {
    float: none;
    display: block;
  }
}
</style>
<body>
<div>
    <% if user_signed_in? %>
    <% if current_user.role == "admin" %>
    
   
    <% end %>
    <% end %>
      <% if user_signed_in? %> 
          <div class="navbar">
     
            <a class="navbar-brand" href="/polls"><strong>Vote for You!</strong></a>
            <a class="active" href="/polls"><i class="fa fa-fw fa-home"></i> Home</a> 
            <a href="/categories"><i class="fa fa-fw fa-database"></i>Category</a> 
            <% if current_user.role == "admin" %>
            <a href="polls/new"><i class="fa fa-fw fa-plus-circle"></i>Create A Poll</a> 
            <% end %>
            <a href="<%=destroy_user_session_path%>"><i class="fa fa-fw fa-sign-out"></i> Log Out</a>
            <li><em>Current User: <%= current_user.email%></em></li>
          </div>
        </li>
        <% else %>
           <div class="navbar">
            <a href="polls/welcome"><i class="fa fa-fw fa-home"></i> Home</a> 
            <a href="/categories"><i class="fa fa-fw fa-tasks"></i> Category</a> 
            <a href="<%=new_user_session_path%>"><i class="fa fa-fw fa-user"></i> Log In</a>
            <a href="/users/sign_up"><i class="fa fa-fw fa-user-plus"></i> Sign Up</a> 
        <li>
            <a href="https://en.wikipedia.org/wiki/Voting"><i class="fa fa-fw fa-wikipedia-w"></i></a>
            <a href="https://www.facebook.com/myvotetoday"><i class="fa fa-fw fa-facebook-official"></i></a>
        </li>
      </div>
        </li>
           <title>PollApp</title>
         </li>
       <% end%>
     </ul>
    </nav>
 </div>
 </div>
</p>
    <title>PollApp</title>
        <% if notice %>
            <p class="alert alert-success"><%= notice %></p>
        <% end %>
       <% if alert %>
           <p class="alert alert-danger"><%= alert %></p>
        <% end %>
     <%= yield %>
  </body>
</html>
